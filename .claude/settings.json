{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"const p=process.env.TOOL_INPUT?JSON.parse(process.env.TOOL_INPUT):{};const f=p.file_path||'';if(f.endsWith('.ts')||f.endsWith('.js')){console.error('\\n[SPEC-CHECK] SPEC.md を確認しましたか？\\n  実装前に SPEC.md の該当セクションを参照し、仕様外の変更であれば先に SPEC.md を更新してください。\\n')}\""
          }
        ]
      }
    ]
  }
}
